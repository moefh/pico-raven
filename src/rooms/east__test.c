#include "room.h"

ADD_ROOM_SCRIPT(east__test);

static void init_room(uint32_t room_id, struct GAME_STATE *game)
{
    (void)room_id;
    (void)game;
}

static void update_room(struct GAME_STATE *game)
{
    (void)game;
}
